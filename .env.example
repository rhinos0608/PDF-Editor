# Example environment configuration for Professional PDF Editor
# Copy to ".env.local" and adjust values for local development.

# Node/Electron environment
NODE_ENV=development

# Electron flags (0 = disabled, 1 = enabled)
ELECTRON_DISABLE_GPU=0
ELECTRON_FORCE_SOFTWARE_RENDERING=0
ELECTRON_DISABLE_HARDWARE_ACCELERATION=0
ELECTRON_ENABLE_LOGGING=0

# App configuration
APP_LOG_LEVEL=info
APP_DATA_DIR=.
# Add other app-specific settings here as needed

# Security placeholders (do not put real secrets here)
API_BASE_URL=
SENTRY_DSN=
ENCRYPTION_KEY=

